package com.codedifferently.apiexamplejava.student;
import com.codedifferently.apiexamplejava.generics.GenericRepository;
import org.springframework.stereotype.Repository;

    public interface StudentRepository extends GenericRepository<Student> {
}
